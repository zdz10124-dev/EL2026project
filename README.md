# 今天吃什么

这个仓库当前保存的是 Flutter 版本的“今天吃什么”应用。

项目目录：

- `today_eat_app/`：Flutter 应用源码
- `今天吃什么.md`：需求说明

运行方式：

```bash
cd today_eat_app
flutter pub get
flutter run
```

如果已经连接了 Android 手机，也可以直接指定设备：

```bash
cd today_eat_app
flutter run -d <device-id>
```

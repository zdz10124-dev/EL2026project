# 开发测试日志 (2026-06-10)

## 一、真机构建修复

### 1.1 移除视频分析功能
- **问题**：`video_thumbnail` 依赖导致额外的编译复杂度
- **操作**：
  - 移除 `pubspec.yaml` 中 `video_thumbnail: ^0.5.3`
  - 删除 `agent_service.dart` 中 `analyzeFoodVideo()` 和 `_extractVideoFrames()`
  - 删除 `llm_service.dart` 中 `callLlmWithImages()`
  - 删除 `capture_screen.dart` 中视频按钮、`_pickAndAnalyzeVideo()`、`_VideoAnalysisScreen`
  - 删除 `meal_repository.dart` 中 `pickVideoFromGallery()`

### 1.2 数据库方案回退 (sqflite_common_ffi → sqflite)
- **问题**：`sqflite_common_ffi → sqlite3` 的 native assets hook 在构建时尝试从 GitHub 下载 ARM64 预编译库，国内网络超时导致构建失败
- **操作**：
  - 移除 `sqflite_common_ffi`、`sqlite3`、`sqlite3_flutter_libs` 依赖
  - `database_service.dart` 改用 `package:sqflite/sqflite.dart`（Android 原生 JNI）
  - `main.dart` 移除 `sqfliteFfiInit()` 调用
- **影响**：当前仅支持 Android/iOS，桌面端 FFI 方案后续通过条件导入单独恢复

### 1.3 Kotlin 编译冲突修复
- **根因**：`package_info_plus 9.0.1`、`shared_preferences_android 2.4.23` 显式 apply `kotlin-android`，与 AGP 9.0.1 内置 Kotlin 冲突，Gradle 并行编译时 FilePageCache 重复注册
- **最终方案**（gradle.properties）：
  ```
  android.builtInKotlin=false
  org.gradle.parallel=false
  org.gradle.workers.max=1
  kotlin.compiler.execution.strategy=out-of-process
  kotlin.incremental=false
  kotlin.daemon.enabled=false
  ```
- **仍存警告**：`package_info_plus` 和 `shared_preferences_android` 仍 apply KGP，需后续升级到 AGP9 兼容版根治

### 1.4 JDK 17 配置
- **原因**：AGP 9.0.1 不支持 JDK 25
- **操作**：`gradle.properties` 中配置 `org.gradle.java.home` 指向 Eclipse Temurin JDK 17

### 1.5 APK 瘦身
- 删除无用依赖 `cupertino_icons`（0 处引用）
- 13 张 UI 背景图压缩到 1080p：34MB → 24MB (-29%)
- Release 构建改为单架构 `--split-per-abi`，arm64 仅 45MB
- 最终：92.6MB → 45.1MB (-51%)

---

## 二、AI 功能 Bug 修复

### 2.1 偏好分析 / 营养分析页面永久转圈
- **根因**：`recordsStream.first` 等待 broadcast stream 的下一个事件，但数据早已 emit 过，新订阅者永远收不到 → Future 永不完成
- **修复**：三个页面改用 `fetchRecords()` 直接获取数据

### 2.2 偏好分析 — 本地缓存与加载优化
- **实现**：
  - `PreferenceAnalysis` 模型新增 `toJson()` 方法
  - 使用 `SharedPreferences` 缓存分析结果 JSON + 时间戳
  - 进入页面优先展示缓存结果（带"上次分析"标记）
  - 无缓存时自动触发首次分析，显示加载提示文案
  - AppBar 右侧提供刷新按钮，刷新时保留旧结果 + 顶部蓝色进度条

### 2.3 营养分析 — 本地缓存与加载优化（同偏好分析模式）
- 缓存按近7天/近30天分别存储，切换时段时自动加载对应缓存
- 加载提示、历史标记、刷新按钮与偏好分析一致

---

## 三、多图上传

### 3.1 数据模型
- `MealRecord.imagePaths: List<String>` — 所有图片路径列表
- `MealRecord.imagePath` 改为兼容 getter（返回首图）
- 数据库新增 `image_paths TEXT` 列，自动迁移旧数据

### 3.2 拍摄页面
- 合并单图/多图按钮为一个（`pickMultiImage` 支持 1-N 张）
- 编辑页 `PageView` 左右滑动 + 圆点指示器
- 首页记录卡片右下角 `+N` 角标

### 3.3 AI 识别结果交互
- 识别结果改为卡片式展示，每条有 ✓（采纳）和 ✗（丢弃）按钮
- 顶部"全部采纳"/"清除"快捷操作
- 多图时支持同时发送给 AI（`analyzeFoodImages`）

### 3.4 全屏图片查看器
- 新建 `widgets/image_viewer.dart`
- 单击任意照片 → 全屏 PageView 左右滑动 + 双指缩放
- 日记卡片和首页卡片均已接入

---

## 四、联网推荐
- 定位添加 8s 超时保护
- 异常提示人性化（中文友好文案）
- 服务器未开放时显示重试按钮

---

## 五、AI 功能使用指南

| 功能 | 入口 | 触发方式 |
|------|------|----------|
| 图片识别 | 首页拍照/选图 → 编辑页 "AI 识别图片" | 手动按钮 |
| 美食日记 | 日记页 → 选日期 → "生成美食日记" | 手动按钮 |
| 偏好分析 | 偏好分析页 | 自动分析（结果缓存 + 手动刷新） |
| 营养分析 | 营养分析页 | 自动分析（近7天/近30天分别缓存 + 手动刷新） |

## 六、当前遗留问题

- [ ] `package_info_plus 9.0.1` 和 `shared_preferences_android 2.4.23` 的 KGP 警告，需升级到 AGP9 兼容版
- [ ] 桌面端 (Windows/Linux) 数据库方案需通过条件导入恢复 `sqflite_common_ffi`


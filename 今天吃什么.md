应用名：今天吃什么
# 技术栈
手机app
使用flutter开发
# 基础功能
1. 拍照记录功能 用户每顿饭前拍下自己的饭，并记录价格、菜名和地点，并且在吃完后可以进行评分。偷偷接入agent，分析食品特点。暂定：食材、辣度、调料、菜系
2. 生成美食日记功能 让ai搓个好看的日记，记录用户每一天，让用户开心开心
3. 统计功能：统计某家店的词数、喜好的菜品等等  喜好内容可以是菜品、菜系、辣度、食材、地区  该部分由ai agent负责，因为从每天的吃饭中提取出共同点难以由程序直接完成
4. 帮助我决定中午或晚上吃什么技能：分两种模式：随机模式和偏好模式 随机模式即在用户已经吃过的菜中随便选一个 偏好模式则根据用户在对应时间常吃的食物或喜欢吃的食物给出推荐
5. 营养分析：让ai看看你营养充不充足
6. 金额统计：统计你从哪天到哪天吃了多少钱
7. 联网推荐：为你推荐其他人吃过的高评分菜品
# 界面描述
下方分四个部件，图标分别为：照相机（拍照记录功能） 一碗面（决定吃什么功能） 类似windows的四个方块组成的田字形图标（其他功能） 齿轮（设置）
## 界面1：拍照记录功能
从上到下，依次为：
- 正中央一个长方形拍照框，读取摄像头。比例与手机原始摄像头一致，尽量大一点。下方一个圆形按钮，上面使用“摄像头”图标。圆形按钮右侧提供一方形按钮，使用“图片”图标。
- 若点击摄像头图标对应圆形按钮：
    - 则：拍下摄像头内容，跳转到编辑界面
    - 编辑界面：长方形拍照框保持不变，只不过里面改为刚刚拍得的图片。下方首先是一个打分星星表，共五颗星星，初始为灰色，用户单击来修改星星数量，支持半颗星星，单击后选中星星变为黄色。下方提供两个可编辑文字框，纵向排列，左侧分别写上提示：“菜品 地点”。下方写上当前手机时间，为月/日/时/分钟 右下侧一个矩形按钮，写上“确定”，左下侧一个矩形按钮，写上“重拍”
    - 若点击“重拍”按钮：
        - 退回最初的拍照界面
        - 如果点击“重拍”前记录了打分、菜品、地点信息，则暂时存在缓存中。如果用户在10分钟内再次拍照，则默认将缓存信息填入菜品、地点框。
        - 仅当用户点击“重拍”或“重选”时使用缓存。如果上一次点击的是“确定”，则下一次重新拍照或重新选图时不使用缓存。
    - 若点击“确定”按钮
        - 将本次拍照信息记录入**数据库**，如果用户没有填写“菜品”或“地点”框，则对应处记录为“未填写”。如用户从来没有点击过星星一栏，则记录”未打分”
        - 退回至拍照主界面
- 若点击图片图标对应方形按钮：
    - 跳转到选择图片界面，用户进入图库选择图片，仅支持选择一张。选择图片界面大致照抄qq、微信发图片界面。右下角按钮文字改为“确定”，左上角提供叉号❌️按钮。
    - 若点击叉号按钮：则退回到拍照主界面
    - 若点击确定按钮，则跳转到编辑界面。与之前的编辑界面不同的是，这里的编辑界面左下角为“重选”按钮。若点击重选按钮，则重新进入选择图片界面，同样缓存已写下的菜品、地点信息。另外一个按钮逻辑相同。
- 在“编辑界面”保留对接智能体的接口，以后有可能实现ai自动根据图片填写菜品，根据gps定位填写地点。
- 当前编辑界面已额外显示一行 GPS 定位结果，位置在“记录时间”下方。右侧提供一个小型“刷新”图标，用于手动重新定位。
- 当前 GPS 定位逻辑以“尽量给出粗略结果”为原则：
    - 实时定位超时时间为5秒
    - 优先返回“市 + 区”
    - 如果拿不到区，则退到“市”
    - 如果拿不到市，则退到“省”
    - 如果实时定位超时，但系统能提供最近一次坐标，则优先尝试用最近一次坐标反查省/市/区
    - 如果连粗略地点都无法反查，则显示坐标文本，避免一直卡在失败状态
## 界面2：今天吃什么界面
- 正中央一个巨大的圆形按钮，中间写上“（待定，不知道写啥）”，下方提供模式选择框。该框请类比一般的软件中选择“语言”的框。点击该框后，下方提供诸多选项，用户可以滑动选择。至少包括：随机模式、偏好模式。 模式选择框下方用文字写上该模式的介绍。随机模式：从数据库中随机选一个“主菜” 偏好模式：从数据库中加权选一个“主菜” 
- 当前偏好模式的具体逻辑为：同一个主菜的多次记录会先累加权重，再按权重进行随机抽取，而不是直接把最高权重菜品固定展示给用户。抽中某个主菜后，再从该主菜的历史记录中随机选一条作为参考记录。
- 当用户从“今天吃什么”界面切换到其他底部页面，再切换回来时，界面会恢复初始状态，不保留上一次推荐结果和参考记录，但模式选择会保留。
## 界面3：其他功能界面
待定，暂时留空处理
- 该界面先按“卡片集合页”设计，整体允许上下滑动。页面最上方写“其他功能”四个字，下方用2列卡片或纵向卡片列表展示各功能入口。每一张卡片都包含：图标、功能标题、一行简介、右侧箭头或“进入”按钮。
- 该界面第一版至少提供以下四个功能卡片入口：
    - “美食日记”：用于查看按天生成的吃饭日记
    - “统计分析”：用于查看金额、常吃菜品、常去地点等统计
    - “营养分析”：用于查看ai给出的营养建议
    - “联网推荐”：用于查看其他人吃过的高评分菜品
- 卡片的排序从上到下优先为：美食日记、统计分析、营养分析、联网推荐。后续若增加功能，也继续往下添加，不影响前四个入口位置。
- 每张卡片点击后跳转到对应二级页面。二级页面左上角提供返回按钮，返回后仍回到“其他功能界面”原来的滚动位置。
- 若点击“美食日记”卡片：
    - 屏幕上方选择起、止时间，单击对应时间可以打开一个日历小窗口，用于选择某一天。
    - 屏幕中央一个按钮：生成美食日记
    - 按钮下方提供一个选择，可以选择日记样式。目前待定只有“标准模式”。该选择框请类比之前“今天吃什么界面”选择“随机模式、偏好模式”的样式。
    - 最下方展示对应样式美食日记的预览。
    - 若点击“生成美食日记”按钮：
        - 跳转到美食日记界面。界面整体是一个单页笔记本样式，初始时在第一页。页面底部标明页码。提供翻页的左箭头和右箭头（无法翻页时对应方向箭头呈现灰色）。
        - 对于某天的记录，在美食日记上的体现如下： 
            - 首先，仍然以“当天凌晨四点到次日凌晨四点”为一天进行分组。
            - 一页最多存放四条记录。
            - 只有“当天的第一条记录”上方需要写日期：年/月/日，下方配一条较浅的分割线。是否写日期只与这条记录是不是当天第一条记录有关，而不与翻页位置有关。
            - 对于每条记录：给出缩小版的图片、菜品记录、地点记录、时间记录。时间只写“小时:分钟”，不重复写日期。
            - 如果某条记录中的菜品或地点在数据库中是“未填写”，则在日记中对应位置直接留空，不显示“未填写”字样。
            - 图片进行随机偏斜旋转，偏转角度在正负25度以内（这些数据都记录在json或xml中方便修改）。
            - 同一页中，图片左右位置采用交错排列。只有每页第一条记录的图片位置是随机决定在左侧或右侧，后续记录按照左右交错排布。
            - 记录文字为横排显示，不使用竖排排版。
            - 当前版本暂未加入装饰物贴图，仅保留后续扩展空间。
        - 不同样式的影响：
            - 整体笔记的贴图
            - 图片的边框
            - 装饰物贴图
- 若点击“统计分析”卡片：
    - 跳转到“统计分析页面”
    - 页面上方提供时间范围选择区域，至少包括：近7天、近30天、自定义时间
    - 下方显示多个统计模块，至少包括：总花费、记录次数、常去地点、常吃主菜、评分分布
    - 若用户点击某一统计模块，例如“常去地点”或“常吃主菜”，则可展开或跳转到详情列表，按次数从高到低显示
    - 页面底部预留给ai统计内容，标题可写“ai观察”，未来用于总结用户偏好
- 若点击“营养分析”卡片：
    - 跳转到“营养分析页面”
    - 页面上方显示“本周/本月营养情况”切换入口
    - 下方先显示一个总评区域，例如“较均衡”“蔬菜偏少”“油炸偏多”等，该区域总评由ai负责生成。目前不接入ai，可以保留接口。
    - 再下方分卡片显示：蔬菜摄入、蛋白质摄入、饮品情况、重口味情况、规律程度 这部分也暂时保留接口。
    - 当前阶段若ai能力尚未接入，则页面显示“功能准备中”，并保留固定位置与布局，避免后续大改
- 若点击“联网推荐”卡片：
    - 跳转到“联网推荐页面”
    - 页面上方提供筛选项，至少包括：地区、菜系、价格区间、评分排序
    - 下方以卡片流形式展示推荐菜品，每张卡片至少显示：图片、菜名、店名、地区、人均价格、推荐理由、评分
    - 点击某一推荐卡片后，跳转到“推荐详情页面”，展示更完整的介绍、图片和推荐理由
    - 若当前未联网或没有推荐结果，则显示空状态提示，例如“暂时没有可用推荐”
- “其他功能界面”本身在用户首次进入时，若相关数据不足，不应报错，而应显示友好的占位文案，例如“先记录几顿饭，再来看看这里的内容”
## 界面4：设置界面
- 第一版先分为以下四个分组：
    - 账号与数据
    - 智能功能
    - 关于应用
- “账号与数据”分组中至少包含以下设置项：
    - “本地数据管理”：进入后可查看当前本地记录总数、图片数量、数据库占用空间。可以删除、编辑某条记录。**注意，删除、编辑某条记录后，修改应同步至数据库**
    - “导出数据”：仅放个按钮样式即可，后续实现将记录导出为表格或其他格式
    - “清空缓存”：用于清除临时缓存，不清除正式记录；点击后必须二次确认
    - “删除全部记录”：高风险操作项，文字可用红色；点击后必须弹出确认框，确认框中再次说明“此操作不可恢复”
- “智能功能”分组中至少包含以下设置项：
    - “图片识别补全菜品”：开关项，当前可以先保留为关闭状态
    - “定位补全地点”：开关项，当前可以先保留为关闭状态
    - “营养分析提醒”：开关项，用于未来提醒用户查看营养总结
    - 提供开关“是否将自己的菜品记录公开”，开启后，用户的记录有可能出现在别人的“联网推荐”页面下
- “关于应用”分组中至少包含以下设置项：
    - “应用版本”
    - “功能说明”
    - “隐私说明”
    - “意见反馈”
- 每个设置项点击后进入对应二级页面，或直接切换开关状态。若是进入二级页面，则二级页面左上角提供返回按钮。
- 设置页面底部显示一行较浅颜色的小字，可写“今天吃什么 by xxx（后续可改）”或“愿你每天都能好好吃饭（后续可改）”，作为页脚占位。
- 若某些设置当前尚未真正接入功能，也要先保留页面位置和交互说明，避免后续设计变动过大。
# 数据库
- 格式：表格形式，分别为 "年" "月" "日" "时" "分钟" "菜品(照抄用户所填)" "地点(照抄用户所填)" （后续地点根据gps记录）"省" "市" "区" (后续内容由ai分析得到)"主菜" "配菜" "饮品" "小吃" "评分(根据用户所填，每颗星星为2分)" 
# 备注
- 上述所有提到的ui界面以及你将要设计的ui界面的所有参数都分离出来，单独写在json或xml格式的文件中，并且提供清晰易懂的中文注释，让从来不了解代码的设计师也能轻易看懂并理解应该如何调节
- 清晰告诉我应该在哪个目录下运行项目


    

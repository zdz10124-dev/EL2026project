# 开发测试日志补充（2026-06-02）

## flutter run 长时间卡在 `assembleDebug`

### 现象
- 真机上可以安装 APK 并打开 App，但前台 `flutter run` 会长时间卡在 `Running Gradle task 'assembleDebug'...`
- 某些场景下后台的 Gradle daemon 实际已经成功产出 APK，但前台客户端没有稳定收到完成消息，所以看起来像是“一直在编译”

### 根因 1：`flutter_secure_storage 10.3.1` 与当前 Android 构建组合不稳定
- `flutter_secure_storage 10.3.1` 的 Android 子工程会单独拉起 `com.android.tools.build:gradle:8.13.2`
- 在当前工程的 AGP 9.0.1 + `android.builtInKotlin=false` 组合下，出现过两类问题：
  - 国内网络下载 `dl.google.com` 超时，导致 `:flutter_secure_storage` 配置失败
  - dex 合并阶段报错：`MasterKey.class ... requires its nest mates ... KeyScheme`

### 根因 2：Gradle daemon / JDK 环境混用导致前台卡住
- 排查时发现 Gradle daemon 有时使用 Android Studio 自带的 JBR 21，有时又使用 JDK 17
- 混用后出现过以下问题：
  - `Could not receive a message from the daemon`
  - `compileFlutterBuildDebug` 阶段 Gradle cache / FileLock NPE
  - 前台 `flutter run` 看起来像 `assembleDebug` 无限卡住

### 修复
- [android/build.gradle.kts](/C:/Projects/2026EL/today_eat_app/android/build.gradle.kts)
  - 在 `allprojects { buildscript { repositories { ... }}}` 中补充 Aliyun Maven 镜像
  - 目的：让插件子工程的 `buildscript` 依赖也走镜像，不再直连 `dl.google.com`
- [pubspec.yaml](/C:/Projects/2026EL/today_eat_app/pubspec.yaml)
  - 将 `flutter_secure_storage` 从 `10.3.1` 回退到 `9.2.4`
  - 目的：避开 10.3.1 在当前 Android/AGP 组合下的 dex 合并问题
- [gradle.properties](/C:/Projects/2026EL/today_eat_app/android/gradle.properties)
  - 恢复：`org.gradle.java.home=C:\\Program Files\\Java\\jdk-17`
  - 新增：`org.gradle.vfs.watch=false`
  - 保留：`android.builtInKotlin=false`、`org.gradle.parallel=false`、`org.gradle.workers.max=1`、`kotlin.compiler.execution.strategy=out-of-process` 等稳定化配置

### 验证方式
1. 停止所有 Gradle daemon：`cd android && .\gradlew --stop`
2. 清理工程状态：删除 `android/.gradle`，执行 `flutter clean`
3. 重新拉依赖：`flutter pub get`
4. 单独执行，不要并行跑多个构建命令：
   `flutter run -d <deviceId>`

### 结果
- 2026-06-02 当次验证中，`flutter run -d 7db231b9` 已成功：
  - `assembleDebug` 约 61.6 秒
  - APK 构建成功
  - 安装成功
  - 真机拉起成功

### 注意事项
- 如果同时并行执行 `flutter run` 和 `gradlew assembleDebug`，可能会在 `compressDebugAssets` 阶段出现重复写入同一资源的错误，例如 `already contains entry 'assets/flutter_assets/...`
- 因此 Android 验证时请保持“单命令串行执行”，不要同时开启多个 Flutter / Gradle 构建

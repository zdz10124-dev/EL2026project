# 经验知识库

## 错误/教训

### 前端

- 分页型美食日记不能只按固定条数切分，必须同时根据页面剩余高度决定是否把最后一条挪到下一页。
- 当前页首屏图片不能依赖用户切页后才显示，预缓存只负责提速而不能决定图片是否能首屏渲染。
- 只要导出入口文案是“分享长图”或“分享 PDF”，它的实际导出范围就必须是整本日记而不是当前页。

## 用户偏好

### 前端

- 联网推荐里的点赞、点踩和举报都应先本地回显再后台同步，而不是每次操作后整页刷新。
- 联网推荐中的距离筛选是互斥区间，所以默认应做成单选而不是多选。
- 联网推荐里的举报按钮应当支持再次点击取消，而不是只能单向累加。

### 架构

- 当交互不是支付等高严谨场景时应先更新本地状态再后台同步服务端，而不是等待服务端成功后才刷新界面。

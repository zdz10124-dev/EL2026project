# 今天吃什么 — 测试说明

## 环境准备

### Flutter 端

```bash
cd today_eat_app
flutter pub get
flutter run          # 默认选设备，或：
flutter run -d windows   # Windows 桌面
flutter run -d android   # Android
```

### 后端（服务器代理模式需要）

```bash
cd backend
pip install -r requirements.txt
export OPENAI_API_KEY="sk-..."
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

---

## 测试内容

### 1. AI 模式切换

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 1.1 | 直接连接模式 — 配置 | 设置 → AI 模型配置 → 选择"直接连接" → 填写 API Key → 保存 | 提示"配置已保存"，"AI 模型配置"显示"已配置" |
| 1.2 | 直接连接模式 — 清除 | 设置 → AI 模型配置 → 清除配置 | 提示"配置已清除"，状态变为"未配置" |
| 1.3 | 服务器代理模式 — 注册 | 设置 → AI 模型配置 → 选择"服务器代理" → 填服务器地址、用户名、密码 → 点击"注册" | 提示"注册成功"，显示"已登录：xxx" |
| 1.4 | 服务器代理模式 — 登录 | 清除后重新进入 → 填已有账号密码 → 点击"登录" | 提示"登录成功" |
| 1.5 | 服务器代理模式 — 登录失败 | 填错误密码 → 登录 | 显示红色错误提示 |
| 1.6 | 服务器代理模式 — 退出登录 | 已登录状态 → 点击"退出" | 回到登录/注册界面 |
| 1.7 | 模式切换后保存 | 在直接连接模式保存 → 切到服务器代理 → 切回直接连接 | 直接连接的 API Key 等配置保留（重新进入配置页应显示之前填写的内容） |

---

### 2. AI 图片识别

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 2.1 | 拍照/AI 识别 | 拍照或选图 → 编辑界面 → 点击"AI 识别图片" | 显示加载中 → 出现 AI 识别结果框，展示主菜、配菜、菜系、辣度、食材等 |
| 2.2 | AI 识别自动填菜品 | 菜品框为空 → 点击 AI 识别 | 识别成功后菜品框自动填入识别结果 |
| 2.3 | AI 识别失败处理 | 断网或 API Key 无效 → 点击 AI 识别 | 显示 SnackBar 错误提示，不崩溃 |
| 2.4 | 视频分析 | 回到主界面 → 点击视频图标 → 选视频 | 跳转到视频分析页面 → 显示分析中 → 展示分析结果 |
| 2.5 | 视频分析失败 | 选无法解析的视频 → 分析 | 显示错误信息，不崩溃 |

---

### 3. 美食日记

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 3.1 | AI 美食日记生成 | 其他功能 → 营养分析旁 → 选日期范围 | 等待 AI 返回 → 展示日记标题、正文、摘要、心情（直接连接需要 API Key；服务器代理需要已登录） |
| 3.2 | 数据不足时的处理 | 新建账号 / 清空记录 → 生成日记 | AI 应返回合理提示，不崩溃 |
| 3.3 | 非 AI 笔记本日记 | 其他功能 → 美食日记 → 选日期范围 → 生成美食日记 | 显示笔记本样式翻页，图片旋转交错排列，页码正确 |

---

### 4. 营养分析

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 4.1 | AI 营养分析 | 其他功能 → 营养分析 → 点击"AI 评估" | 显示总评、蔬菜/蛋白质评分、建议 |
| 4.2 | 7天/30天切换 | 营养分析页 → 切换本周/本月 | 数据刷新（不同时间范围应有不同结果） |
| 4.3 | AI 未配置时 | 清除 AI 配置 → 营养分析 | 显示"需先配置 AI 模型" |

---

### 5. 偏好分析

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 5.1 | AI 偏好分析 | 其他功能 → 偏好分析 | 展示喜爱菜系、食材、菜品等标签 |
| 5.2 | 数据不足时 | 只有 1-2 条记录 → 偏好分析 | AI 应提示数据不足，不崩溃 |

---

### 6. 统计分析

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 6.1 | 7天/30天/自定义 | 其他功能 → 统计分析 → 切换时间范围 | 总花费、记录次数、常去地点、常吃主菜、评分分布正确变化 |
| 6.2 | 展开详情 | 点击"常去地点"等卡片 | 展开显示详细排序列表 |
| 6.3 | 空数据 | 无记录时进入 | 显示 0 条、无数据，不报错 |

---

### 7. 联网推荐

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 7.1 | 搜索推荐 | 其他功能 → 联网推荐 | 显示推荐列表（需服务端有数据） |
| 7.2 | 筛选 | 按距离、价格区间筛选 | 列表按条件过滤 |
| 7.3 | 推荐详情 | 点击某推荐卡片 | 跳转到详情页，展示完整信息 |
| 7.4 | 空状态 | 服务器无数据 / 断网 | 显示"暂时没有可用推荐" |

---

### 8. 决定吃什么

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 8.1 | 随机模式 | 决定吃什么 → 随机模式 → 摇一摇 | 从历史记录抽一个菜品，显示名称和原因 |
| 8.2 | 偏好模式 | 偏好模式 → 摇一摇 | 按权重抽取，显示菜品、权重值和参考记录 |
| 8.3 | 无记录时 | 清空所有记录 → 摇一摇 | 提示"先记一顿饭吧" |
| 8.4 | 模式切换记忆 | 切换底部 tab 再回来 | 模式选择保留 |

---

### 9. 拍照记录

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 9.1 | 拍照 | 点击相机按钮 → 拍照 | 进入编辑界面，显示刚拍的照片 |
| 9.2 | 选图 | 点击图片按钮 → 选图 | 进入编辑界面 |
| 9.3 | 评分 | 点击星星评分 | 评分可交互，支持半星 |
| 9.4 | 保存 | 填写菜品/地点/价格 → 确定 | 记录保存到数据库，回到主界面，最新记录显示 |
| 9.5 | 重拍/重选 | 编辑界面 → 重拍/重选 | 保留已填信息（10分钟内），重新拍照/选图 |
| 9.6 | 金额校验 | 输入非数字金额 → 确定 | 弹出"金额格式有误"提示 |
| 9.7 | GPS 定位 | 编辑界面 → 查看 GPS 行 | 显示定位中 → 显示位置（需设备支持） |
| 9.8 | 缓存逻辑 | 填信息 → 重拍 → 10秒内再进入编辑 | 上次填的信息自动填入 |

---

### 10. 设置

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 10.1 | 本地数据管理 | 设置 → 本地数据管理 | 显示记录总数、图片数、数据库大小、记录列表 |
| 10.2 | 编辑记录 | 记录列表 → 三点菜单 → 编辑 | 进入编辑页 → 修改 → 保存 → 列表刷新 |
| 10.3 | 删除单条记录 | 三点菜单 → 删除 | 确认后记录删除 |
| 10.4 | 清空缓存 | 清空缓存 → 确认 | 提示"临时缓存已清空" |
| 10.5 | 删除全部记录 | 删除全部记录 → 确认 | 所有记录和图片删除，列表为空 |
| 10.6 | 公开记录开关 | 开启/关闭 | 开启后触发同步，关闭后停止 |

---

### 11. 后端（服务器代理模式专用）

| 编号 | 测试项 | 步骤 | 预期结果 |
|---|---|---|---|
| 11.1 | 健康检查 | `curl /health` | `{"success": true, "data": {"status": "healthy"}}` |
| 11.2 | 注册 | `POST /v1/auth/register` | 返回 token |
| 11.3 | 重复注册 | 再次注册相同用户名 | 409，提示用户名已存在 |
| 11.4 | 登录 | `POST /v1/auth/login` | 返回 token |
| 11.5 | 错误密码 | 错误密码登录 | 401 |
| 11.6 | AI 代理 — 无 token | `POST /v1/ai/chat` 无 Authorization header | 401 |
| 11.7 | AI 代理 — 正常 | 带 token 发 AI 请求 | 返回 OpenAI 格式 response |
| 11.8 | 上传记录 | `POST /v1/recommendations/upload-record` | 返回 remote_id |
| 11.9 | 搜索推荐 | `POST /v1/recommendations/search` | 返回 paginated 列表 |

---

## 兼容性测试

| 平台 | 说明 |
|---|---|
| Android 真机 | 完整功能测试，尤其 GPS 定位、拍照 |
| Android 模拟器 | GPS 需手动设置坐标 |
| Windows 桌面 | 不支持 GPS（定位行应显示不可用），其余功能正常 |

---

## 回归检查

每次修改后运行：

```bash
flutter analyze
```

必须零问题通过才能提交。

## 已知限制

- 视频分析提取 5 帧，长视频可能遗漏关键内容
- GPS 定位在室内或无 GPS 设备上可能失败
- 服务器代理模式下大图片上传受带宽限制
- 美食日记笔记本非 AI 版本目前只支持"标准模式"一种样式
